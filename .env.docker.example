# Пример .env файла для Docker deployment
# Скопируйте этот файл в .env и заполните значения

# ============================================
# Telegram Bot Configuration
# ============================================
# Получите токен от @BotFather в Telegram
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
# Эти переменные используются Docker Compose для создания БД
POSTGRES_USER=botuser
POSTGRES_PASSWORD=your_secure_password_here_change_me
POSTGRES_DB=telegram_bot

# DATABASE_URL генерируется автоматически в docker-compose.yml
# Не нужно менять для Docker deployment

# ============================================
# Redis Configuration
# ============================================
# REDIS_URL генерируется автоматически в docker-compose.yml
# Не нужно менять для Docker deployment

# ============================================
# OpenAI Configuration
# ============================================
# Получите API ключ на https://platform.openai.com
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================
# Webhook Configuration
# ============================================
# Для production: ваш домен с HTTPS
# Для локальной разработки: ngrok URL
# Примеры:
#   Production: https://bot.example.com
#   Development: https://abc123.ngrok.io
# Для production: ваш домен с HTTPS
# Если домена нет — оставьте пустым и включите USE_POLLING=1
WEBHOOK_URL=https://your-domain.com

# Polling mode (альтернатива webhook — домен не нужен)
# 1 = бот сам запрашивает обновления у Telegram
# 0 = Telegram присылает обновления на WEBHOOK_URL
USE_POLLING=0

# ============================================
# Application Configuration
# ============================================
# Начальный баланс токенов для новых пользователей
# 10 токенов = 2 High качество или несколько Medium
INITIAL_TOKENS=10

# ID приватного канала для мониторинга контента пользователей
# Формат: -1001234567890 (получите ID через @userinfobot)
# Бот должен быть админом канала
# Оставьте пустым чтобы отключить мониторинг
MONITORING_CHANNEL_ID=

# ============================================
# Примечания
# ============================================
# 1. Для локальной разработки используйте ngrok:
#    ngrok http 8000
#    Затем обновите WEBHOOK_URL на URL от ngrok
#
# 2. Для production обязательно:
#    - Используйте сильный пароль для POSTGRES_PASSWORD
#    - Настройте HTTPS для WEBHOOK_URL
#    - Не коммитьте .env файл в git
#
# 3. После изменения .env перезапустите контейнеры:
#    docker-compose restart
